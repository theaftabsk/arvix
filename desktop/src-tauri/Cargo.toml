[package]
name = "arvix"
version = "1.0.0"
description = "ARVIX AI Personal Assistant Native Windows Client"
authors = ["ARVIX Core Team"]
edition = "2021"

[build-dependencies]
tauri-build = { version = "1.5", features = [] }

[dependencies]
tauri = { version = "1.5", features = [ "shell-open", "notification" ] }
serde = { version = "1.0", features = ["derive"] }
serde_json = "1.0"

[features]
custom-protocol = ["tauri/custom-protocol"]
